extends layout

block content
  h1= title
  p Welcome to Sai Swathi Thota Webapps page #{title}
  button(type="button", onclick ="goFaster()" class="btn btn-secondary") Press Me
  div(id="faster") Increase the speed 
  script.
    var change = 1;
    var mph =20;
    function goFaster() {
      mph = change+mph;
      change = change+1;
      document.getElementById("faster").innerHTML = `MPH ${mph} with change ${change}`
    }
